let int uni;
let bool buni;

func void hue (int n) {
    while (n > 0) {
        write("Testing not true");
        buni = true;
        write(not buni);
        n = n - 1;
    }
}

write("Testing -5");
uni = 5;
write(- uni);
hue(5);
